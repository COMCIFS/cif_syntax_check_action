name: 'CIF syntax check'
description: 'Check that all *.cif/*.dic files in the top-level directory conform to CIF syntax'
outputs:
        result:
                description: 'Result of syntax check'

runs:
        using: 'docker'
        image: 'Dockerfile'
